# Flow audit — 2026-08-18 (production pass 2)

> **Historical planning record.** Captures the design as planned; it may diverge
> from what shipped. `LICENSE` and `THIRD_PARTY_NOTICES.md` are authoritative for
> licensing, and the code is authoritative for behaviour.

Flow-oriented production audit: 14 user-journey tracers + adversarial verification.
46 findings confirmed (9 P0 + 37 P1) — all fixed in commits 7599ab3 and 726ad7a.
0 refuted. Below: the P2 polish backlog (not yet fixed) and late tracer findings
whose verification wave was cut short (treat as unverified claims).

## P2 backlog (verified severity, unfixed)

- **startHandsFree is a silent no-op while finalizing/transcribing/inserting** — Click menu 'Start Hands-Free Dictation' (or jot://start-hands-free) during the several-second processing window of the previous dictation: nothing happens — no pill change, no hint, no queued start. User clicks ag
- **Menu offers no way to stop a hands-free session; title stays 'Start Hands-Free Dictation' while recording** — During a locked session (especially after the notice-wipe hides the pill, or on the onboarding-incomplete path) the user opens the status-bar menu to stop — the only dictation item still reads 'Start Hands-Free Dictation
- **Start and lock earcons stack back-to-back on every UI-started hands-free session** — Click the Dictate dot or menu item: the .start earcon (warming) and .lock earcon (recording(false)→recording(true)) fire within milliseconds of each other — a doubled blip on every hands-free start, unlike the single sou
- **UI and mic-live moment blocked at session start on Bluetooth input: engine.start() runs synchronously on the main actor** — AirPods as default input: press fn and start talking — HFP capture activation can take hundreds of ms to ~2s inside AVAudioEngine.start(), which runs synchronously in beginSession on the main actor. The pill and start ea
- **Same-device config rebuild loses the rebuild-window audio without recording a gap marker** — Bluetooth codec renegotiation or a sample-rate change on the current mic mid-recording: engine stops, rebuild takes real time, audio in that window is lost — but unlike the device-switch branch, no gapMarker is appended,
- **stop()/rebuild data race: finalize during an in-flight config rebuild mutates engine/converter from two threads unsynchronized** — Fast user releases fn at the exact moment an AirPods reconnect rebuild is executing on the write queue: stop() (main thread) runs tearDownEngine while buildAndStartEngine (write queue) is assigning self.engine/self.conve
- **Changing the dictation key mid-hold strands the session: releasing the old key no longer finalizes** — Hold fn (recording starts), and while still holding, pick a different key in Settings → General → Dictation key. setKey resets keyIsDown but the processor stays .pressed. Release fn: the flagsChanged event's keyCode no l
- **Saving/removing an API key mid-session stomps the live status glyph and repositions the live pill** — Start hands-free dictation, open Settings → Advanced, paste a key, Save & Validate. When validation lands (~1s), applySettingChange('apiKey') calls activateEngine() unconditionally: onStatusItemState(.idle) kills the lis
- **'Never keep audio' doesn't purge audio for RetryQueue/RecoveryScanner recoveries — audio lingers up to 6 hours** — Retention = Never. Dictate offline → queuedForRetry (audio rightly kept). Network returns, RetryQueue drains: process() writes transcript + awaitingChip via store.upsert (RetryQueue.swift:109-114) — the immediate-purge h
- **Delete All History during a RetryQueue drain resurrects a ghost row after the wipe** — Several queued dictations are draining (visible as 'Waiting for network'). User clicks Privacy → Delete All History → Delete Everything while one upload is in flight. deleteAll removes that record's folder (only the coor
- **Model/endpoint overrides save every keystroke — a retry drain firing mid-edit permanently fails queued dictations** — User is typing a new transcription-model name in Advanced ('every edit saves as you type', SettingsWindow.swift:425-435) while dictations sit queued offline. Network restores mid-edit; RetryQueue drains using the half-ty
- **Closing Settings mid key-validation swallows the 'key didn't work' verdict when replacing an existing key** — User has a stored (revoked) key, pastes its replacement, clicks Save & Validate, and closes the window while the spinner runs. The Task completes after close: on failure keyStatus=.invalid renders nowhere and nothing els
- **Letterless wrong forms (digits/symbols) trigger ALL-CAPS propagation and SHOUT the replacement** — User adds rule "$100" → "one hundred dollars" (or "24/7" → "around the clock"). Dictation containing "$100" inserts "pay ONE HUNDRED DOLLARS now" — fully uppercased. Probe confirmed.
- **100-term prompt cap silently drops terms — UI promises "spelled right in every dictation" with no cap messaging** — User (or a CSV import — cap is 1000) grows the dictionary past 100 terms. Term #101+ (oldest non-starred) never rides the cleanup prompt (DictionaryStore.swift:75 prefix(100)); if it has no misspelling mapped it has zero
- **1000-row import cap truncates silently — "Imported 1000 words" with no mention 500 were skipped** — Import a 1,500-row CSV. Probe confirmed importCSV returns exactly 1000; the footer says "Imported 1000 words" (DictionaryView.swift:185) and the remaining rows vanish with no signal that the import stopped at a cap rathe
- **Header sniff swallows a real first-row term named "Word", "Term", or "Phrase"** — Headerless CSV starting `Word,werd` (teaching it the app name "Word"): probe confirmed only 1 of 2 rows imports — the "Word" entry is treated as a header and silently discarded; feedback says "Imported 1 word" so the use
- **Submitting a whitespace-only term shows the false message "Already in your dictionary"** — Type spaces into "Add a word or phrase…" (field is non-empty so the + button enables and Enter submits) → footer flashes "Already in your dictionary" for a term that is not, and nothing else explains why the add failed.
- **Chained rules rewrite each other's output: A→B plus B→C turns dictated A into C** — Rules "foo"→"bar" and "bar"→"baz" exist. Dictating "foo" inserts "baz", violating the explicit foo→bar rule. Probe confirmed: apply([foo→bar, bar→baz], "foo and bar") → "baz and baz".
- **Overlapping footer feedback: an earlier message's 3s timer wipes a newer message early** — Trigger "Already in your dictionary", then within 3s run an import → "Imported 12 words" appears but vanishes when the first message's timer fires, potentially well under a second after it appeared.
- **Menu items are never validated: 'Start Hands-Free' is a silent no-op during any active session (and there is no Stop item), 'Paste Last Transcript' is a silent no-op when empty** — (a) Start hands-free from the menu, then reopen the menu to stop it — the only related item still says 'Start Hands-Free Dictation' and clicking it does nothing (no sound, no pill change, no log visible to user). There i
- **Menu status line never reflects live state, and the animated glyph freezes while the menu is open** — Start dictating, then click the menu-bar icon mid-recording: the status line reads 'Ready — hold fn to dictate' while the app is actively listening — the menu lies about the most important state. Simultaneously the liste
- **Success badge word count undercounts smart-formatted transcripts (splits on spaces only)** — Dictate a ~30-word grocery list; smart formatting returns it as newline-separated lines ('1. Milk\n2. Eggs\n…'). The success badge's 'N words' (shown when >20 words) undercounts by one word per line break because words j
- **Quit has no termination hook: quitting mid-recording silently swallows the dictation until the next launch; quitting during insertion can leave the transcript stuck on the clipboard** — (a) User is 2 minutes into a recording and hits Quit in the menu (⌘Q equivalent): the app dies instantly with no confirmation and no finalize; the words only resurface at the NEXT launch when RecoveryScanner auto-transcr
- **Back-to-back notices overwrite each other — launch-time recovery notice can be clobbered by the retry-drain notice before it is readable** — Launch after a crash while also having offline-queued dictations: RecoveryScanner posts 'Recovered your last dictation…' (4s), then RetryQueue drains and posts 'N queued dictations are ready…' which instantly replaces th
- **Pill panel relies entirely on NSHostingView transparent hit-test pass-through; never sets ignoresMouseEvents even when fully hidden (hide() is dead code)** — With 'show idle indicator' off, the pill state is .hidden (EmptyView) but the 600×96 panel at .screenSaver level stays ordered front at the bottom-center of the last-used screen — hide() exists but is called from nowhere
- **Increase Contrast / Differentiate Without Color not honored anywhere on the pill — borderless glass with variant-on-glass text** — User with 'Increase Contrast' enabled: the pill remains a borderless Liquid Glass capsule (explicitly 'No borders, no edge glows — the glass edge is the only edge') with onSurfaceVariant text over translucent glass; over
- **Long notice text can overflow its capped capsule (fixedSize vs maxWidth 560 inside a 600pt panel)** — The auto-degrade notice 'Smart formatting paused — cleanup kept misfiring. Re-enable in Settings → Dictation.' (~85 chars) renders near or past the 560pt cap: the Text refuses to truncate or wrap (fixedSize), so the glas
- **finalizeSession runs side effects on a REJECTED transition: fn press-and-release during transcribing/inserting rewrites the live session's meta to failed/'no_audio' (flashes 'Transcription failed' in History; downgrades crash recovery)** — Dictate; while the pill shows processing, hold fn ≥0.3s and release (the natural 'start next dictation' gesture whose .begin was dropped). The release emits .finalize; finalizeSession proceeds despite the state machine r
- **'Paste Last Transcript' is a silent no-op in common states and discards its insertion outcome (secure field → absolutely nothing happens)** — (a) Relaunch the app: lastResult is in-memory only, so the menu item does nothing even though History is full of transcripts — no notice, item never disabled. Same after Delete All (clearLastResult) and on first run. (b)
- **Overlapping guarded pastes can leave the clipboard completely EMPTY, destroying the user's original clipboard** — Clipboard holds the user's content X. A dictation inserts via paste (restore pending for 1s). Within that second a second guarded paste runs — realistically 'Paste Last Transcript' clicked from a menu opened during trans
- **Success badge word count splits on spaces only — undercounts multi-line transcripts (regression of the audit-L29 class)** — Dictate a long multi-paragraph passage (smart formatting inserts newlines). Pill success badge shows e.g. '61 words' for an 80-word transcript because words separated by newlines count as one token; 'line one\nline two' 
- **'Ready to paste' History chip claims clipboard state forever — stale the moment the clipboard changes** — Trigger the chip flow (switch apps mid-dictation) but miss the 5s pill notice. Days later History still shows the row badged 'Ready to paste' although the clipboard was overwritten dozens of times — clicking the row does
- **A single 5xx or bad-endpoint DNS error pauses the whole retry drain as 'still offline' — queued rows sit at 'Waiting for network' indefinitely while online** — Two dictations queued (offline). Wi-Fi returns; drain starts; the first item hits a transient Gemini 500 (after the service's one silent retry). The drain declares 'still offline — pausing' and stops. Network is healthy,
- **TimeoutPolicy connect/TTFB/stall budgets are never wired up — a dead-air stall runs ~31s, and the silent retry doubles it to ~60s+ of 'Still working…'** — Wi-Fi drops mid-upload in the hanging way (no RST — captive portal, AP power-save). The design says give up 10s after first-byte silence; actually the request idles the full overall deadline (31s for a short clip), then 
- **429 without a parsable short retryDelay is reported as 'Daily quota reached' and excluded from auto-retry** — Hit a per-minute rate limit whose response lacks Retry-After / has retryDelay > 8s (or a date-format Retry-After header, which Double() can't parse). Pill: 'Daily quota reached — saved to History' — wrong claim about the
- **After fixing a bad API key, auth-failed dictations never auto-recover — each row needs a manual Retry** — Key revoked; three dictations fail with 'API key isn't working — saved to History' (attention icon). User pastes a fresh key in Settings → Advanced; menu status flips to Ready and the attention icon clears — but the thre
- **RecoveryScanner's 'manual retry' rows for older interrupted sessions are auto-uploaded seconds later by the launch drain** — App crashes with several interrupted sessions on disk. On relaunch the scanner auto-transcribes the most recent ('quota-respectful — only the most recent') and marks older ones for manual retry — but the launch drain tha
- **Network failure during the empty-transcript re-send is swallowed and misclassified as a non-retryable 'Couldn't transcribe'** — First transcribe call returns an empty transcript on real speech; the connection drops (or key is revoked, or a 429 hits) during the automatic re-send. The error is swallowed into an empty string, so the session surfaces
- **Architecture doc advertises jot://start|stop|toggle — 'start' and 'toggle' don't exist** — An automation author (or future Raycast extension) follows docs/design/architecture.md and wires `jot://start` or `jot://toggle`. Nothing happens — the command falls into the default: branch with only a log
- **start-hands-free during an active fn hold silently locks the session; during transcribing it is a silent no-op** — (a) User is mid push-to-talk hold; a Raycast start-hands-free fires. The pill abruptly switches to the locked layout (lock icon, timer, stop button) while their finger is still on fn — releasing still finalizes correctly
- **First-open onboarding deep link may land on Welcome instead of screen n (jump posted before SwiftUI subscribes)** — With the onboarding window not yet open, `open jot://onboarding/4` opens the window on the Welcome screen instead of jumping — repeating the command (window now exists) jumps correctly. Breaks the stated headless-
- **"Never (disables Retry)" is wrong in both directions — Retry still works for the rows that need it, while what actually breaks is unmentioned** — User avoids the most private retention option because the label (and PRIVACY.md:37-38 '"never" disables Retry') says Retry dies. Reality: failed/offline-queued rows KEEP their audio under 'Never' (purge requires transcri
- **Crash recovery that fails on quota/auth shows "Waiting for network" while the user is online** — App relaunches after a crash with an interrupted recording; auto-recovery transcription hits daily quota (or the 403/400 key problems above). The row is marked queuedForRetry, so History's attention shelf titles it "Wait
- **"Ready to paste" chip on drained/recovered rows — ⌘V pastes something else because the clipboard was never populated** — A queued dictation drains (or crash-recovery restores one): notice correctly says 'it's in History', but the History timeline row wears a "Ready to paste" chip. User hits ⌘V and gets whatever was on the clipboard — the d
- **"Stored in your Mac's Keychain and only ever sent to Google" becomes false with an endpoint override — the key goes to whatever host is configured** — User sets a proxy/staging endpoint in the same Advanced pane whose key-section footer (and onboarding screen 2) asserts the key is 'only ever sent to Google'. applyAuth attaches x-goog-api-key to every request against th
- **History detail sheet leaks raw internal status identifiers ("awaitingChip", "queuedForRetry", "heldSecure")** — Any record's detail sheet, Status row: user reads camelCase enum values instead of words; 'awaitingChip' is meaningless outside the codebase, and 'failed' appears with no reason while the errorCode that would explain it 
- **Hard-coded "fn" in History empty state and onboarding Done screen — wrong once the user picks a different dictation key** — User sets the dictation key to Right ⌘ in General settings. History's empty state still coaches 'Hold fn and say hello.' and a re-run of onboarding ends with 'Hold fn anywhere and start talking.' — holding fn now trigger
- **Menu-bar status line reads "Starting up…" for the entire onboarding flow — and forever if onboarding is closed early** — First run: while the user works through onboarding (or closes it with the red button before saving a key), the menu shows the disabled line 'Starting up…' indefinitely — the app is not starting up, it's waiting on the us
- **Onboarding mic screen claims recording happens "only while you hold the dictation key" — hands-free mode and the mic test itself contradict it** — Privacy-relevant permission prompt says 'Jot records audio only while you hold the dictation key.' The product also records via hands-free lock (Space-while-holding, idle-dot Dictate button, menu 'Start Han
- **Saving an API key mid-session stomps live session UI: status glyph flips to idle and the recording pill jumps to the mouse's screen** — Start hands-free dictation, wander to Settings → Advanced (recording continues), Save & Validate a key. The listening/processing menu-bar animation is killed (setState(.idle)) mid-session, the status line rewrites to "Re
- **A failed validation of a NEW key hides "Remove Key…" and the stored-key prompt even though a working key remains stored** — Stored working key. User tests a candidate key that's bad → Save & Validate → invalid. The Remove Key… button disappears, and once the field is cleared the prompt reads "Paste your key" — the pane now claims no key is st
- **saveFailed path silently destroys the previously stored key while the menu still says Ready** — Stored working key; user saves a new key; validation passes but SecItemAdd fails (keychain locked/entitlement edge). Pane says "The key validated but couldn't be saved — try again", but the OLD key was already deleted by
- **Model-override per-keystroke commit turns an abandoned half-edit into live config → every dictation permanently fails with no pointer back** — Advanced → Transcription model → type "gemini" → get interrupted, close the window. The partial override is committed. Every dictation now 404s → badRequest → terminal "Couldn't transcribe — saved to History" rows (Retry
- **Dictionary CSV import is a silent no-op for unreadable/non-UTF-8 files — the exact "broken button" disease a77d98d claims to have cured** — Import a CSV exported from Excel in Windows-1252/Latin-1 (accented names — precisely what a personal dictionary holds). String(contentsOf:encoding:.utf8) fails → importCSV() returns before any feedback: no "Imported N", 
- **Dictionary feedback timer race: a second message is wiped early by the first message's 3s timer** — Trigger two feedbacks within 3s (e.g. dup-add at t=0, import at t=2.5): the first showFeedback's sleeping Task fires at t=3 and nils `feedback`, erasing the second message after ~0.5s instead of 3s. Related: whitespace-o
- **NetworkReachability.probablyOnline blocks the main thread (up to 1s) from onboarding key validation** — Onboarding key screen, validation fails → NetworkReachability.probablyOnline() runs synchronously on the MainActor: semaphore.wait up to 1s while NWPathMonitor's first callback arrives on a background queue. Typically a 
- **Launch drain auto-uploads every older interrupted session, contradicting the scanner's own "older = manual retry" / quota-respectful design** — Several crashes over a week leave 4 interrupted folders. Relaunch: scanner auto-transcribes the newest (by design), marks the 3 older ones .queuedForRetry — then RetryQueue's launch drain, started immediately after, uplo
- **Manual Retry racing the scanner's in-flight auto-recovery double-uploads the same audio and races two FLAC encodes on one file path** — Relaunch after crashing mid-way through a long (multi-minute) recording. While RecoveryScanner's transcription is in flight (can be 30s+), open History — the row already shows in the attention shelf as "Recovered recordi
- **Kill during inserting: recovered row is chipped "Ready to paste" but recovery never puts the text on the clipboard** — App crashes mid-insertion (transcript present). Relaunch shows "Recovered your last dictation — it's in History"; the History timeline row carries a "Ready to paste" chip. User hits ⌘V trusting the chip — whatever stale 
- **API key removed between crash and relaunch: recovery fails into a permanently non-auto-retryable "Transcription failed" row, and the recovery notice is invisible during onboarding** — Crash mid-recording; API key deleted (or invalidated) before relaunch. On relaunch needsOnboarding is true so the HUD panel is never ordered front, yet the scanner still runs: auto-transcribe fails auth → queuedForRetry 
- **0-byte/corrupt CAF recovery: queued → immediately re-failed as "retry_unexpected", leaving a Failed row with a Retry that can never succeed; crashed blips resurface as Failed** — Kill the app within ~0.5s of starting a dictation (or corrupt audio.caf). Relaunch: scanner's FLAC encode throws, the catch-all marks it queuedForRetry; the launch drain instantly re-fails it ("retry_unexpected"). The us
- **Sleep mid-recording: dictation finalizes into a transcription that resolves at wake against a stale or locked-screen context, with the outcome toast flashed while the screen is off/locked** — Hold fn dictating, close the lid. willSleep finalizes (stop earcon plays as the lid shuts) and the network task races the sleep. If it completes/resumes at wake while the lock screen is up, SecureInput is active → the re

## Late tracer findings (UNVERIFIED — re-check before fixing)

- [P0/high] **Paste path holds the session in .inserting for ~1s AFTER text visibly lands — next dictation silently dropped (words lost), success feedback late, Esc eaten system-wide** — Dictate into any paste-path app (Chrome, Slack, VS Code, Terminal — everything in AppQuirks.forcePaste/needsManualAccessibility). Text appears at the cursor ~0.11s into insert(), but PasteInserter.pas
- [P1/high] **Focus-change guard evaporates at tier 2: AX pid-mismatch falls through to a blind ⌘V into whatever has keyboard focus (launcher overlays, mid-flight app switch)** — Repro A: dictate into app A; while the pill shows processing, summon Raycast/Alfred/Spotlight (nonactivating panels — NSWorkspace.frontmostApplication still reports A, so guard 1 passes). Insert() run
- [P1/high] **Secure-field block after successful transcription says 'Can't dictate into a password field' — never mentions the words were saved; History chip just says 'Held'** — Dictate a paragraph into a normal field; while transcription runs, focus lands in a password prompt (or Terminal's Secure Keyboard Entry is on, or another app left secure input stuck — TN2150). Transc
- [P2/high] **finalizeSession runs side effects on a REJECTED transition: fn press-and-release during transcribing/inserting rewrites the live session's meta to failed/'no_audio' (flashes 'Transcription failed' in History; downgrades crash recovery)** — Dictate; while the pill shows processing, hold fn ≥0.3s and release (the natural 'start next dictation' gesture whose .begin was dropped). The release emits .finalize; finalizeSession proceeds despite
- [P2/high] **'Paste Last Transcript' is a silent no-op in common states and discards its insertion outcome (secure field → absolutely nothing happens)** — (a) Relaunch the app: lastResult is in-memory only, so the menu item does nothing even though History is full of transcripts — no notice, item never disabled. Same after Delete All (clearLastResult) a
- [P2/medium] **Overlapping guarded pastes can leave the clipboard completely EMPTY, destroying the user's original clipboard** — Clipboard holds the user's content X. A dictation inserts via paste (restore pending for 1s). Within that second a second guarded paste runs — realistically 'Paste Last Transcript' clicked from a menu
- [P2/high] **Success badge word count splits on spaces only — undercounts multi-line transcripts (regression of the audit-L29 class)** — Dictate a long multi-paragraph passage (smart formatting inserts newlines). Pill success badge shows e.g. '61 words' for an 80-word transcript because words separated by newlines count as one token; '
- [P2/high] **'Ready to paste' History chip claims clipboard state forever — stale the moment the clipboard changes** — Trigger the chip flow (switch apps mid-dictation) but miss the 5s pill notice. Days later History still shows the row badged 'Ready to paste' although the clipboard was overwritten dozens of times — c
- [P0/high] **"I'll add it later" + finishing onboarding leaves a dead app whose menu says "Starting up…"** — Fresh install → onboarding → API key screen → click "I'll add it later" (OnboardingWindow.swift:270) → grant mic + accessibility → finish Globe/TryIt/Done → click "Start dictating" (whose copy literal
- [P0/high] **Try It screen is inert on every true fresh install — hotkey engine is never running during onboarding** — Fresh machine → onboarding → paste API key on screen 2 → grant mic (3) and accessibility (4) → reach "Try it." (OnboardingWindow.swift:451: "Click into the field below, hold your key…"). Holding fn do
- [P0/high] **Menu 'Start Hands-Free Dictation' in any pre-activation state records invisibly with no way to stop** — Fresh install → close onboarding at the welcome screen (or leave it open) → status-bar menu → "Start Hands-Free Dictation" (always enabled). OS mic prompt appears out of nowhere; after Allow, recordin
- [P0/high] **Closing onboarding mid-flow strands the app silent; later permission grants are never rechecked until relaunch** — Fresh install → red-button close the onboarding window at any screen before completing setup. App state: menu status line "Starting up…" (a lie — nothing is starting), icon plain idle (not attention),
- [P1/high] **Mic screen is a hard dead end after the user denies the permission prompt** — Fresh install → mic screen → Grant → click Deny on the TCC prompt (or mic already denied/restricted from a prior run). The card still shows "Grant"; clicking it again does nothing at all — no prompt, 
- [P1/high] **"I'll add it later" users get full 7-screen onboarding re-run at every launch — auto-started at login by default** — User skips the key, completes everything else, finishes onboarding (launch-at-login checkbox defaulted ON and registered, OnboardingWindow.swift:501,508-516). Next login: the app auto-launches and the
- [P2/high] **Done screen hardcodes "Hold fn" regardless of the configured hotkey** — User who changed the dictation key to right-⌘ in Settings re-runs onboarding via jot://onboarding (or a user whose fresh default ever ships non-fn): Done screen says "Hold fn anywhere and start
- [P2/low] **Re-run onboarding mic screen runs a second capture engine concurrently with live dictation** — User with a fully working install opens jot://onboarding, sits on the mic screen (meter engine live, OnboardingWindow.swift:337-345), then holds fn to dictate — the coordinator starts its own A
- [P0/high] **Esc during transcribing/finalizing discards the entire recording regardless of length — minutes of words destroyed** — Dictate 30+ seconds, release fn. While the pill shows processing (2-4s transcription window), press Esc — e.g. to dismiss an autocomplete popup in the target app; the event tap consumes it. The sessio
- [P1/high] **Hold-release during an in-flight session clobbers its meta to failed/no_audio — History lies mid-transcription and errorCode pollution persists** — Dictate sentence 1, release. While it transcribes (or during the paste path's .inserting window), hold fn >=0.3s to start sentence 2 and release. The release fires .finalize; finalizeSession runs agai
- [P1/high] **Paste path pins the app in .inserting for ~1.2s AFTER the text visibly lands — chained dictations in that window are silently swallowed** — Dictate into Terminal/iTerm (forcePaste apps always take the paste path — Ammaar's dogfood environment). Text appears ~150ms after processing ends (100ms settle + Cmd-V), but the state machine stays i
- [P1/high] **Secure-input outcome never tells the user their words survived — and blames a 'password field' even when the cause is Terminal Secure Keyboard Entry** — A) Secure input activates mid-dictation (sudo/Touch ID prompt appears, user clicks a password field, screen-lock): after a long dictation the pill shows 'Can't dictate into a password field' for just 
- [P2/medium] **Success pill shown when Cmd-V lands nowhere (no focused text field), then the restore removes the only pasteable copy** — Dictate while the target app has no focused text field (reading a webpage, Finder, a video call): AX fails (no readable value → falls through), Cmd-V is posted into nothing, paste() returns true, pill
- [P2/medium] **Frontmost-app guard is checked once — a Cmd-Tab during the ~150-350ms insert window pastes the transcript into the wrong app** — User releases fn, and right as processing finishes Cmd-Tabs to another app (natural while waiting). Guard 1 passed against the old app; the AX tier's pid check correctly falls through (AXInserter.swif
- [P2/high] **'Paste Last Transcript' menu item: silent no-op when empty, and all fallback outcomes are swallowed** — First run (or after Delete All History, which correctly clears lastResult — verified at DictationController.swift:305): the menu item is enabled, user clicks it, absolutely nothing happens — no paste,
- [P2/high] **History 'Ready to paste' chip is a permanent stale promise — still shown days after the clipboard was overwritten** — Dictate, switch apps mid-flight → chip flow → row stored as awaitingChip. User never pastes; the clipboard is overwritten minutes later by normal use. A day later History still badges the row 'Ready t

